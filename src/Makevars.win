PKG_FFLAGS = $(FPICFLAGS)
PKG_LIBS = $(FLIBS)

# Windows-specific settings
FC = gfortran
FFLAGS = -O2 -fno-second-underscore -fbounds-check -fbacktrace

%.o: %.f
	$(FC) $(FFLAGS) $(PKG_FFLAGS) -c $< -o $@